{{> header}}

<section id="all-receipt-view">
    <h1>All Receipts</h1>
    <div class="table">
        <div class="table-head">
            <div class="col wide">Creation Date</div>
            <div class="col wide">Items</div>
            <div class="col wide">Total</div>
            <div class="col">Actions</div>
        </div>

        {{#each receipts}}
            {{> receipt}}
        {{else}}
            <div class="row">
                <p>No receipts in database.</p>
            </div>
        {{/each}}

    </div>
</section>

{{> footer}}