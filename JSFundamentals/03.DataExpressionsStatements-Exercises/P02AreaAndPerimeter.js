function calcAreaAndPerimeter(length, width) {
    let area = length * width;
    let perimeter = 2 * (length + width);

    console.log(area);
    console.log(perimeter);
}

calcAreaAndPerimeter(2, 2);
calcAreaAndPerimeter(1, 3);
calcAreaAndPerimeter(2.5, 3.14);
